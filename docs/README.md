# Vite sample usage

This folder contains a minimal Vite app that demonstrates how to use `react-simple-maps`.

## Run locally

```bash
cd docs
npm install
npm run dev
```

Then open the local URL printed by Vite (usually `http://localhost:5173`).

## Build

```bash
cd docs
npm run build
npm run preview
```
